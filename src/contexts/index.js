import { createContext } from "react";

//export const MovieBillboardCtx = createContext(null)
export const MovieDetailsCtx = createContext(null);
export const PeopleDetailsCtx = createContext(null);
export const MatrixCtx = createContext(null);
export const ConfigPreferencesCtx = createContext(null);
export const PreferencesCtx = createContext(null)
